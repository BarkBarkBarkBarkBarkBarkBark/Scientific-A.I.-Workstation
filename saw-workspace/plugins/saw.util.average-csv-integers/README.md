# Average (CSV Integers)

Computes the arithmetic mean of a comma-separated list of integers.

## Params
- `csv_integers` (string): e.g. `"1, 2, 3"`
- `on_invalid` (string): `error` (default) or `ignore`

## Outputs
- `average` (number)
- `count` (number)
- `sum` (number)
